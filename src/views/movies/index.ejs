<%- include('../partials/head', { title: 'Listado de películas' })  %> 
	<h2>Listado de películas</h2>
	<ul>
		<% movies.forEach(movie => { %>
		<li>
			<b>Title:</b> <%= movie.getTitleAndRating() %> <br>
			<b>Genre:</b> <%= movie.genre ? movie.genre.name : 'Sin género' %> <br>
			<a href="/movies/<%= movie.id %>">Ver detalle</a>
			<hr>
		</li>
		<% }) %>
	</ul>
<%- include('../partials/footer')  %> 
